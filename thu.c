#include <stdio.h>
#include <string.h>
#define MAX 10000
int main() {
    char T[MAX+1], P[MAX+1];
    int count = 0;

    scanf("%s",s);
    fgets(T,MAX,stdin);
    for (int i=0;T[i]!='\0';i++)

 printf("%d",count);
}
}
